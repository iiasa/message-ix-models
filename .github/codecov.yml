comment:
  layout: "diff, files"
  behavior: once

coverage:
  precision: 1
  status:
    project:
      default:
        if_ci_failed: success
    patch:
      default:
        if_ci_failed: success
