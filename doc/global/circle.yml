dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install -y python-sphinx
    - sudo apt-get install -y texlive-xetex
    - sudo pip install sphinxcontrib-bibtex
    - sudo pip install cloud_sptheme
    - sudo pip install sphinxcontrib-fulltoc
    - sudo apt-get update

test:
  override:
    - xelatex --version
    - make html
## this is broken for now
## something with the cloud theme causes it to fail
## but only on circle, I can still build locally (for now).. 
#    - make xetexpdf
