dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install -y python-sphinx texlive-extra 
    - sudo apt-get install -y texlive-full texlive-fonts-recommended texlive-fonts-extra texlive-bibtex-extra texlive-xetex
    - sudo pip install sphinxcontrib-bibtex
    - sudo pip install cloud_sptheme
    - sudo pip install sphinxcontrib-fulltoc

test:
  override:
    - make html
    - make xetexpdf
